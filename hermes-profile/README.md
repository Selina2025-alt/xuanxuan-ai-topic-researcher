# Hermes 运行 Profile

`SOUL.md` 是从项目 Profile 生成的脱敏公开副本。它不包含密钥、真实飞书链接或个人绝对路径。

创建 Profile 后运行：

```bash
hermes profile show topicresearchagent
```

根据输出确认实际 Profile 目录，再把 `SOUL.md` 复制到该目录。不要在未确认安装路径时凭经验写入。
